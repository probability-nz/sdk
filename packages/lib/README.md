# @probability-nz/lib

Non-React utilities and types for [Probability](https://probability.nz) plugins. Used by [`@probability-nz/react`](../react); also usable on its own (Node, server-side validators, non-React clients).

## Build

```sh
pnpm build
```
