# @probability-nz/analog

JSON schema for Probability Automerge documents and presence.

Produces `dist/analog.json` — a JSON Schema 2020-12 file derived from
the types in `@probability-nz/types`.

## Build

```sh
pnpm build
```
